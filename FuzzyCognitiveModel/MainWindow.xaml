<Window x:Class="FuzzyCognitiveModel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:FuzzyCognitiveModel.Views"
        mc:Ignorable="d"
        Title="Fuzzy cognitive map" Height="450" Width="800" Background="#FF999999">

    <Grid>
        <Menu Height="40" VerticalAlignment="Top">
            <Button Content="Button" Width="75" Click="Button_Click"/>
        </Menu>
        <views:ConceptsControl DataContext="{Binding FuzzyCognitiveViewModel}" Margin="10,45,396,20"/>
        <views:FuzzyCognitiveMatrixUser x:Name="FCM" DataContext="{Binding FuzzyCognitiveViewModel}" Margin="0,45,10,100" HorizontalAlignment="Right" Width="329"/>
        <!--<views:GraphControl x:Name="GraphControl" DataContext="{Binding FuzzyCognitiveViewModel}" Margin="0,45,10,20" HorizontalAlignment="Right" Width="381"/>-->
    </Grid>
</Window>
